# The wrapper must never absorb files from a canonical repository exposed here.
/local/*
/remote/*

# Machine-specific configuration and local boundary links.
/config/orbit.conf

# Local operating-system and tooling noise.
.DS_Store
.AppleDouble
.LSOverride
._*
.codex/
